const KOREAN = {
  CLOSER_DEADLINE: "마감임박순",
  RECENT_ENROLLMENT: "최근등록순",
};

const FUND = {
  CLOSER_DEADLINE: "CLOSER_DEADLINE",
  RECENT_ENROLLMENT: "RECENT_ENROLLMENT",
};

Object.freeze(KOREAN);
Object.freeze(FUND);

export default { KOREAN, FUND };
