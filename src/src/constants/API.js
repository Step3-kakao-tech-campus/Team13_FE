const AUTH = {
  LOGIN: "/login",
  SIGN_UP: "/signup",
  USER_SETTING: "/user/setting",
};

const FUND = {
  GET_LIST: "/posts",
};

const USER = {
  SETTING: "/user/setting",
};

Object.freeze(AUTH);
Object.freeze(FUND);
Object.freeze(USER);
export default { AUTH, FUND, USER };
