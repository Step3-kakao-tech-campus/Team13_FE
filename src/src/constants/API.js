const AUTH = {
  LOGIN: "/login",
  SIGN_UP: "/signup",
};

const FUND = {
  GET_LIST: "/posts",
};

const QUERY_KEY = {
  POST_LOGIN: "postLogIn",
};

Object.freeze(AUTH);
Object.freeze(FUND);
Object.freeze(QUERY_KEY);
export default { AUTH, FUND, QUERY_KEY };
